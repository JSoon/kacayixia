import React from 'react';
import Footer from '../components/Footer/Footer';

const App = ({
    children
}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default App;